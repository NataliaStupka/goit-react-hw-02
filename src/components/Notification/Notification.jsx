import s from './Notification.module.css';

const Notification = () => {
  return (
    <div>
      <p className={s.textNotification}>No feedback yet.</p>
    </div>
  );
};

export default Notification;
